language: cpp
compiler: gcc
sudo: required
dist: trusty
branches:
  except:
    - gh-pages

before_install:
  # Install apt-get for qt 5.10.1
  - sudo add-apt-repository -y ppa:beineri/opt-qt-5.10.1-trusty
  # For g++
  - sudo add-apt-repository -y ppa:ubuntu-toolchain-r/test
  # Update
  - sudo apt-get update -qq

install:
  # Install qt
  - sudo apt-get install -y qt510-meta-minimal
  # Setup qt
  - source /opt/qt510/bin/qt510-env.sh
  - export QTHOME=/opt/qt510
  # Install g++
  - sudo apt-get install g++-6
  # Install doxygen
  - sudo apt-get -y doxygen
  - sudo apt-get -y doxygen-doc
  - sudo apt-get -y doxygen-latex
  - sudo apt-get -y doxygen-gui
  - sudo apt-get -y graphviz

script:
  - cd src
  - ../script/build.sh

deploy:
  provider: pages
  skip-cleanup: true
  github-token: $GITHUB_TOKEN
  keep-history: true
  on:
    branch: master
